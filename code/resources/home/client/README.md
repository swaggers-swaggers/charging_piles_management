# 客户端首页轮播图片

图片统一放在此目录。当前程序会随安装包携带 `slide_01.jpg`、`slide_02.jpg`，按文件名排序自动轮播；替换这两个同名文件后请重新构建客户端。

支持 `.jpg`、`.jpeg`、`.png`、`.webp`，建议使用横向图片，推荐尺寸 `1600 × 900` 或更高。若要新增第三张及更多图片，需要同时把文件名加入 `code/resources/res.qrc` 后重新构建。
